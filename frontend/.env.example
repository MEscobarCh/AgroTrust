# URL base del backend AgroTrust (Flask)
# En desarrollo local con Vite proxy, se puede dejar vacío o comentar para usar el proxy '/api'
# En producción (Vercel), debe configurarse con la URL pública del backend desplegado (ej. en Render, Railway, etc.)
VITE_API_BASE_URL=http://127.0.0.1:5000
